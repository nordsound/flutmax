out 0 (audio_l): signal;
out 1 (audio_r): signal;

wire notein_obj = notein();
wire note = notein_obj.out[0];
wire freq = mtof(note);
wire osc = cycle~(freq);
wire quiet = *~(osc, 0.2);
out[0] = quiet;
out[1] = quiet;
